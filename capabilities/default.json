{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the inspector window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
